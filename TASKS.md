- [x] compact pane header (especially on mobile, want it to take up little space, right now is too much)
- [x] wire up to real api (`lib/api.ts` sembleApi → https://api.semble.so/xrpc, x-api-key auth; unset NEXT_PUBLIC_USE_MOCK to use it)
- [x] when not logged in, the seed cards are pulled randomly from the global feed (recent 200 activities)
- [x] handling trails saved to semble - for now trails that have been saved to semble become read only
- [x] save to semble popup for naming and describing
- [x] filter out links already in trail from pane lists
- [x] option to refresh entire seed list of 3 at once
- [x] when logged in, the seed links should come from the signed in users library (randomly selected from most recent 100)
- [x] Bigger bottom trail bar + expandable view (becomes bigger and verticals)
- [x] option to delete trails
- [x] Tighten up design and pane ux (fitting phone screen better) (e.g. fill up the screen and scrolling panes is more like a gallery with the floating dots to show which one on)
- [x] first seed pane can take a text box "what's been on your mind lately?" (with some multi-select option similar to selecting some tags when first signing up to a social network) - if the user enters a query (or selects some options which get concatenated into a query) then the seed link is `https://semble.so/search/cards?query={query string with + instead of space}` (e.g.`https://semble.so/search/cards?query=how+do+agents+collaborate+on+the+same+codebase`)
- [x] simplify homepage to be calm, uncluttered and centred: just the prompt / seed "slots" (and let's make it prompt / text field with topic pill selection - with more options e.g. rewilding the internet, open social, online community creation, etc - which should cycle through on refresh or refresh button) then below it can be something like "or seed your trail with random links" - make it clear its one or the other - then from this home page we begin the pane ux trail walking as it currently is. but the home page should feel inviting and clear.
- [x] home page of a shared trail should be similar to above too (either no links selected and 3 random ones are selected as the "seed" - with the shared collection as the 'root' seed)
- [x] home page should always be the prompt trail page and whenever we start a trail then we create a route with the trail id (so whenever we go to root, its always the prompt page) - for viewing a shared trail, we see the shared trail at the url that was shared, then when a link is clicked, it goes to the trail building view (and the route to that newly created trail)
- [x] we need to make the trail view (at the bottom and expanded) nicer (at the bottom is not so useful, so maybe we just hide that) and the expanded should take up the whole screen height (rather than just partial, and the cards should look like other url cards) - the bottom view is nice on desktop with a big view, but on mobile its not so useful, so we can either hide it on mobile dimensions or they can expand to the full trail view.
- [x] there should be a button at the bottom of the screen when building a trail "finish trail" and then it pulls up a complete trail page / form, showing the trail (as vertical list) and a 2 field form to prompt the naming of the title and description of this trail. then a "publish trail to semble" button / flow and then a share to bluesky.
- [x] i also want a mode at the end, when finishing the trail building which is "pairing" mode e.g. "which two links stood out most to you?" or "which two links had the most unlikely connection? why?", where the user can optionally select a url card from the final trail, and another (select to highlight) then prompted to say something about it.
- [x] lets get rid of the "step 1" at the top of the pane
- [x] lets get rid of the hover effect when hovering over a url card in a pane list. it currently moves the who card. instead, we can just have it either shade a bit, or the border thickens, or some other best practice but i don't want it to move, that jitter is not pleasant.
- [x] lets find a cleaner way to include the "finish trail" button - maybe the way it is is fine, but then the horizontal list of dots should probably go to the top - or the bottom bar becomes the dots, with the arrow to expand (no need for text "walked path" "a question" "2 steps" etc)
- [x] lets remove the pill filters and source pills in the panes (e.g. no floating filters at the top and no need to include the source "mutual" "similar" "connected" in the link card)
- [x] top right button should be a button that starts a new trail (which just goes to the home prompt page) instead of being "restart trail button"
- [x] the home prompt page should be fully visible, and not need a scroll to see the bottom (e.g. the example prompt pills take up too much space)
- [x] Finish button should move like the bottom footer when side panel opens
- [x] Expanded trail view link card border outline covers too much when clicked (the border covered the number on the left)
- [x] Meet the people curating these links (list of users who curated these links) - on the finish trail window, show a list of all the users that curated these links (for each link in the trail, get the list of users who saved it to their library, deduplicate the list
- [x] Change the seed links to be just one - it picks one randomly. And it should pick a random one each time that button on the home prompt page is clicked (right now it keeps whatever was last selected)
